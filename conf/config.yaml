defaults:
  - model: ks_mlp
  - data: ks
  - training: default
  - wandb: default
  - _self_

seed: 42

hydra:
  run:
    dir: outputs/${wandb.group}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: false
